# Memory — 2026-02-28

## Evening session fixes (Feb 28 ~10:30pm ET)

### Ash code review — items resolved
- **#1 (prev_reading_pos centering)**: Already fixed in both train.py and nws_obs_trader.py. Ash reviewed old code. ✅
- **#2 (between market disambiguation)**: Already proven not a real issue in other session. Left as-is. ✅
- **#3 (settle_all CLI fallback)**: Removed. Now Kalshi-only settlement — if Kalshi hasn't finalized, skip. Simpler and correct.
- **#4 (dewpoint fallback consolidation)**: `extract_dewpoint(peak_obs, obs_seq)` added to trading_utils.py. Both nws_obs_trader.py and disambiguation/algorithms.py now import from there.
- **#5 (raise MIN_EDGE)**: Skipped per Ian — not needed.
- **#6 (peak_idx first vs last match)**: Deferred. No two obs should have same timestamp anyway.
- **DST bug**: Already fixed — lst_midnight() uses fixed UTC offsets, bypasses ZoneInfo entirely. Not a concern for March 8. ✅

## Evening session (Feb 28)

### Ash code review findings — resolved
- #1 (`prev_reading_pos` centering): already fixed in code before Ash reviewed. Non-issue.
- #2 (between market disambiguation): already validated as non-issue. Left as-is.
- #3 (settle_all): simplified to Kalshi-only settlement. CLI fallback removed. If Kalshi hasn't finalized, trade skips.
- #4 (dewpoint fallback): consolidated into `extract_dewpoint()` in `trading_utils.py`. Both `nws_obs_trader.py` and `disambiguation/algorithms.py` now import from there.
- #5 (MIN_EDGE): skipped — don't raise it.
- #6 (peak_idx first-match): defer. Two obs shouldn't share the same timestamp anyway.
- DST fix: already in via `lst_midnight()` using fixed UTC offsets. March 8 concern is moot. ✅

---

## Session summary (late night, ~1am-2:17am ET)

### Disambiguation model — full rebuild in progress
Sub-agent running: `build_training_data.py --days 365`
Log: `/tmp/build_td_v3.log`
Session key: `agent:main:subagent:548565ab-f3be-4caf-a27c-c0a6fb2c42bc`
DB: `polymarket/disambiguation/training.db` (wiped, being rebuilt)
Expected runtime: 60-90 min from ~2:17am ET

**What changed vs v1:**
- CLI source: AFOS (45 days) → IEM daily.json (365 days, exact match verified)
- New fields in obs_readings: `dewpoint_c`, `wind_knots`
- Reconstruction: asos1min now fetches `tmpf,dwpf,sknt` (verified formula matches NWS)
- LST midnight fix applied throughout
- DB wiped and restarting clean

**When sub-agent finishes:**
1. Run `verify_subagent.py` — checks 3 spot checks (window alignment, CLI match, outcome dist)
2. Verify dewpoint/wind populated >= 80%
3. Rebuild model with new features (see below)
4. Run `train.py --save`

### Model improvements planned (not yet built)
Priority order:
1. **City identity feature** — one-hot or city embedding; some cities have structural HI bias
2. **30-60 min slope** — temp trend over hour before peak (not just instantaneous rise_rate)
3. **Dewpoint depression** (temp_c - dewpoint_c at peak) — dry air = faster spikes
4. **Wind speed at peak** — high wind = mixing = less spiking
5. **HI+1 as third class** — LO/HI/HI+1 (not binary) — ~45 real physical miss cases validated
6. **Two-stage model**: T+0 (slope+dewpoint+wind+city, available immediately) and T+30 (adds time_in_bucket, next_drop)

### Key findings tonight
- IEM `daily.json` returns exact NWS CLI values, 365 days/year in one request per city
  - Network codes: MA_ASOS, IL_ASOS, FL_ASOS, TX_ASOS, VA_ASOS, OK_ASOS, CO_ASOS, NV_ASOS, CA_ASOS, WA_ASOS, PA_ASOS, AZ_ASOS, MN_ASOS, LA_ASOS, GA_ASOS
- "9% MISS rate" was mostly data bugs (partial-day records from NWS 7-day window edge)
- Real physical MISS: +1F overshoot, ~45 verified cases with full-day coverage (LV, Dallas, etc.)
  - Mechanism: 1-min peak spikes 1°F above 5-min average, smoothed away
  - LO/HI/HI+1 is the correct framing
- Dewpoint + wind reconstruction verified: asos1min dwpf/sknt → same formula → matches NWS ✅
- wethr.net UI uses 1-min ASOS + CLI data in retrospect; live API = same 5-min °C we already have
- DST bug fixed: `lst_midnight()` in trading_utils.py, applied to nws_obs_trader + obs_exit_monitor
- Backtest results added to STRATEGY_IDEAS.md:
  - Whale copy-trading: NO SIGNAL (lift=0.984)
  - Buy the favorite @ 11am: NO SIGNAL overall; 50-60% confidence tier shows +9.5% ROI (keep in mind)

### Disambiguation accuracy so far
- Binary (LO/HI), 453 samples, 7 features: **59% CV accuracy** (52% base rate)
- Best model: LogReg C=0.1
- After rebuild: expect 1500+ samples + dewpoint/wind/city features → target 65%+

### Two-stage trading decision
- **T+0** (first peak reading): slope + dewpoint depression + wind + city → get in fast
- **T+30** (confirmed): adds time_in_bucket + next_drop → confirmation signal
- Build two separate models for each stage

### Backtests run tonight
- `reports/whale_backtest.md` — NO SIGNAL
- `reports/buy_favorite_backtest.md` — NO SIGNAL overall, 50-60% tier interesting

### Code committed: `34b0a21`
All changes pushed to github.com/code3z/trading main branch.
