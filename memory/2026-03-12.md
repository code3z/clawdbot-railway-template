# Memory — 2026-03-12

## CLI Trader: Between-Market Proximity Fix (commit a935f52)
- **Bug**: `find_edge_trades` hardcoded `proximity=0` for ALL between markets, so deep-ITM buckets (e.g. [65-66] when confirmed=72) sorted ahead of boundary markets ([71-72]) and the single `trades[0]` order went to the illiquid bucket
- **Fix**: Proximity now = distance from confirmed_high to nearest bucket edge:
  - In-bucket YES → 0
  - Above bucket NO → confirmed_high - cap_s
  - Below bucket NO → floor_s - confirmed_high
- Boundary markets now correctly win the sort

## CLI Trader: Multi-Order Async Architecture (commit b7a9efa)
Per Ian's direction: submit ALL qualifying tickers, not just best[0]. Speed is key.

**New flow:**
1. `find_edge_trades` now returns `(guaranteed_trades, gate_trades)` — two separate lists
2. **Guaranteed trades** (submit immediately, no gate): confirmed_high already clears threshold
   - `greater` YES when confirmed > floor
   - `between` NO when confirmed > cap (already above bucket)
   - `less` NO when confirmed >= cap (already at/above cap — currently dead code in `_market_direction`, pre-existing gap)
3. **Gate-dependent trades** (submit only if 3-part gate passes):
   - `between` YES: confirmed in bucket (peak must be done to stay in range)
   - `between` NO above: confirmed < floor (gate confirms peak won't reach bucket)
   - `greater` NO: confirmed ≤ floor (gate confirms peak won't exceed threshold)
   - `less` YES: confirmed < cap (gate confirms peak won't reach cap)
4. `_submit_all(trades, label)` uses `ThreadPoolExecutor` — all orders fire concurrently
5. `_submit_one(trade)` is thread-safe: acquires `spend_lock` for dedup + budget, releases, then calls API

## CLI Trader: Bug Fix (commit 57e3428)
- `event_source` variable only set inside `if confirmed_high is not None` block
- Gate-reject log at bottom of `trade_city` used `event_source` — NameError on NWS polling path
- Fixed: use `cli_result.get("source", "unknown")` instead

## Known Pre-Existing Gap (not introduced by this session)
- `_market_direction` only returns YES for `greater` and `less` markets — never NO
- Means `greater NO` and `less NO` guaranteed trades are never generated
- e.g. confirmed=72, greater floor=75 → bet NO (high won't reach 75) → valid guaranteed win, skipped
- Not a regression; pre-dates this session; can fix in a future pass

## _tier_to_tags Bug Fix (commit 2b0c30f, earlier in session)
- Wildcard import `from nws_obs_signals import *` skips underscore-prefixed names
- `_tier_to_tags` lived in `nws_obs_confirmed.py` but wasn't reaching `nws_obs_trader.py`
- Every `observation` event (`peak_passed`, `trough`, `confirmed`, `late_night`) was crashing silently
- Only `new_high` events worked (different code path)
- Fix: added explicit import line in `nws_obs_trader.py`
- Broken since the March 10 file split — signals were dead for ~1 day before caught

## HEARTBEAT.md Log Fix
- HEARTBEAT.md was referencing `nws_obs_trader.stdout.log` for error checking
- That daemon was unloaded March 10; the real errors are in `wethr_push_client.stdout.log`
- Fixed log reference in HEARTBEAT.md

## CLI Push Trigger: Confirmed Paper-Only
- `com.trady.cli-push-trigger` restarted with PID 83518 (earlier in session)
- Script on disk had `paper=True` but daemon was running old version; now confirmed paper-only

## Open: Disable Variant B in twc_morning_trader.py
- Still not done — lowest priority from this session but still pending
- Variant B = below-floor NO for HIGH markets; cold bias works against us; not backtested

## Commits this session
- `a935f52` — between proximity fix
- `b7a9efa` — multi-order async architecture (guaranteed vs gate-dependent split)
- `57e3428` — event_source NameError fix
- `2b0c30f` — _tier_to_tags explicit import

---

## TWC DST Bug — Root Cause & Fix (commit 2a3e091, ~midnight March 12)

### The Bug
`twc_forecast.py` had `_dst_info()` returning `True` only on the ONE day clocks spring forward (March 8). The disputed-window correction (fetching hourly temps for midnight→1AM LDT) only fired on that single day. For the remaining ~230 days of DST, `is_dst_day=False` → correction dead → **every DST day, the NWS settlement window extends 1 hour past what TWC's calendarDay covers, and we miss it**.

### The Fix
Added `_is_dst_active(target_date, tz_name)` — checks `ZoneInfo(tz_name)` DST offset at noon of that day. Returns True for ALL DST-active days. Arizona/Hawaii automatically return False (no DST). Both `get_twc_forecast` and `fetch_twc_lst` now use `_is_dst_active` instead of `is_dst and dst_dir == +1`.

### Verified live (NYC March 12)
- `_is_dst_active("2026-03-12", "America/New_York")` → True ✅
- `_is_dst_active("2026-03-12", "America/Phoenix")` → False ✅ (no DST in AZ)
- NYC: cal_lo=54°F → lst_lo=37°F (disputed-window correction working)
- Denver, Chicago: cal_lo unchanged (no cold front in disputed window)

### calendarDayTemperatureMin — authoritative definition confirmed
From TWC API docs example: `temperatureMin=59, calendarDayTemperatureMin=65` for the same day. `calendarDayTemperatureMin` is **strictly midnight-to-midnight** local. `temperatureMin` is the night period (7 PM → 7 AM next morning), extends past midnight, therefore colder. Ian was right.

### Why cal_lo=54°F vs hourly min=37°F for NYC March 12
**Model staleness, not definition.** Daily forecast updates infrequently (set by most recent model run, probably yesterday evening). Hourly updates more frequently. The cold front arriving mid-morning March 12 was already in the hourly by midnight, but the daily cal_lo still reflected the older run. Both are midnight-to-midnight; just different model runs.

### Implication for our correction
The disputed-window fix does two things:
1. Extends the settlement window by 1 hour (midnight LDT → 1 AM LDT) — the main intent
2. Implicitly uses a fresher forecast source (hourly vs daily) — bonus accuracy

### Pre-existing test failure (NOT from our changes)
`test_obs_cache_integration.py::test_fetch_obs_uses_cache_when_fresher` — 1 failing test that pre-dates this fix. 153/154 passing, same as before.

## Variant B re-evaluation (post-compaction)
- Ian confirmed we DID backtest Variant B in the P-table recalibration (`backtest_twc_ptable.py`)
- Results: δ=1: 70.1%, δ=2: 78.2%, δ=3: 87.8%, δ=4: 93.2%, δ=5+: 93.9%
- Conclusion: min-edge filter + calibrated P-table handles it naturally — no need to disable Variant B
- "NOT backtested" framing was wrong; it meant only the original 87-signal backtest, not the P-table work

## Test fix: obs_cache integration tests (commit 269209f)
- All 4 tests in `test_obs_cache_integration.py` used civil `ZoneInfo("America/New_York")` for `today_str`
- `fetch_obs` internally uses `lst_today()` (UTC-5 fixed, no DST)
- Between midnight and 1 AM EDT during DST, civil date = "next day" but LST date = "today"
- `date_str == local_today` check in `fetch_obs` was failing → cache silently skipped during this window
- Fix: all tests now use `lst_today("America/New_York")` — consistent with production code
- 154/154 passing after fix

## CLI push trigger restarted post-session
- Restarted to pick up all session code changes (async submission, guaranteed/gate-dep split)

---

## TWC LST Window Refactor (commits 52ed0eb, c1d40e7, ~1:30 AM March 12)

### What changed
`fetch_twc_lst` and `get_twc_forecast` in `twc_forecast.py` now use the full LST-day
hourly window for temperature correction, not just the 1-hour disputed window.

**Old behavior:** only checked disputed 1 hour (midnight LDT D+1 → 1 AM LDT D+1).
Missed TWC daily model staleness for the rest of the day.

**New behavior:**
- LST window = 1 AM LDT D → 1 AM LDT D+1 during DST (standard = midnight→midnight)
- Fetch TWC hourly for the FULL window; 15-min substitutes last hour when within range
- `final_lo = min(cal_lo, min(lst_window_temps))`
- `final_hi = max(cal_hi, max(lst_window_temps))`
- Falls back to cal values for dates >48h out (hourly doesn't reach)
- Hourly and 15-min fetches moved OUTSIDE the date loop (one call per city)

### Why max for highs is correct (verified empirically)
Tested all 19 cities — every correction moved in the right direction:
- Lows: all DOWNWARD corrections (cold front hitting tonight — Chicago −11°F, OKC −20°F, Austin −16°F)
- Highs: all UPWARD corrections (warm rebound on March 13 after front passes — Dallas +11°F, Austin +11°F, OKC +8°F)
The "stale-warm daily, cooler hourly for highs" case doesn't appear in practice. When fronts hit, lows drop but next-day highs RISE. `max` captures both correctly.

### TWC daily self-corrects within ~90 min after midnight
NYC cal_lo was 54°F at midnight EDT; by 1:27 AM EDT it had updated to 36°F. The 18°F staleness gap was real but temporary. The correction still provides value for:
1. Cities that haven't yet updated (Chicago −11°F persisted through the test)
2. Catching warm-rebound highs before the daily updates

### Bugs fixed in same session (commit c1d40e7)
- `__main__` block crashed on `r["disputed_high"]`/`r["disputed_low"]` — keys renamed to `lst_corrected`/`dst_active`
- Both `get_twc_forecast` and `fetch_twc_lst` had unused `lst_off = LST_OFFSETS.get(city, -5)` — removed
- Stale docstrings updated throughout

### API call count increased
`fetch_twc_lst` now makes 4 calls per city (daily + hourly + 15-min + intraday) vs 2 before.
76 calls across 19 cities per forecast_logger run. TWC trial key handles this fine.

### 154/154 tests passing after all changes

### Key result dict fields
`fetch_twc_lst` result per date: `{"high": float, "low": float, "cal_high": float, "cal_low": float, "is_dst": bool}`
`get_twc_forecast` result: adds `lst_corrected`, `dst_active`, `high_source`, `low_source`, `high_segment`, `low_segment`

## Morning tasks (March 12)
- Settle 6 live fills from March 11 (IDs 2563–2568) via trade-settler at 9 AM ET
- Spot-check new `twc_m_market` orders have `market_price != 0.99`
- Monitor fill_monitor for backfilled twc_low_dip March 11 trades (IDs 2842–2873)

---

## Kalshi API Breaking Changes — March 12, 2026

Kalshi rolled out broad field renames overnight March 11→12. Three separate fixes required.

### 1. Orderbook endpoint: `orderbook` → `orderbook_fp` (fix ~5 AM, commit b866afa)
- `GET /markets/{ticker}/orderbook` now returns `{"orderbook_fp": {"yes_dollars": [...], "no_dollars": [...]}}`
- Old: `{"orderbook": {"yes": [[price_cents, qty], ...], "no": [...]}}`
- `yes_dollars`/`no_dollars` entries are `[price_str, qty_str]` with prices as dollar strings (e.g. `["0.45", "100"]`)
- Fix: Added `_normalize_orderbook()` helper in `trading_utils.py` — tries `orderbook` first, falls back to `orderbook_fp`, converts dollar strings to int cents
- Fill monitor was logging ~40+ API failures per sweep since 3:36 AM

### 2. Trades endpoint: `yes_price`/`no_price` → `yes_price_dollars`/`no_price_dollars` (fix ~5 AM, commit eab0b31)
- `GET /markets/trades` now returns `yes_price_dollars` (str like `"0.0300"`) and `no_price_dollars` instead of int cents
- Also: `count` → `count_fp` (str like `"2.00"`)
- Fix in `paper_trading/fill_monitor.py` `fetch_trades_since()` — supports both formats

### 3. Portfolio orders endpoint + market prices — same pattern (fix 11:15-11:30 AM)

**Orders (`GET /portfolio/orders`):**
- `fill_count` (int) → `fill_count_fp` (str like `"1.00"`)
- `remaining_count` (int) → `remaining_count_fp` (str)
- `yes_price`/`no_price` (int cents) → `yes_price_dollars`/`no_price_dollars` (str)
- Fix: Added `_order_fill_count()`, `_order_remaining_count()`, `_order_price()` helpers in `twc_morning_trader.py` (commit 1035ee9)
- Without fix: `fill_count` = 0 → `_mark_filled` guard fires → every live fill silently skipped → reconcile marks all trades expired

**Market prices (`GET /markets`):**
- `yes_ask`/`no_ask` (int cents) → `yes_ask_dollars`/`no_ask_dollars` (str)
- `parse_market()` in `trading_utils.py` was returning `None` for EVERY market → 0 candidates for all traders
- Fix in `parse_market()`: tries `yes_ask` first, falls back to `yes_ask_dollars` (commit 6372fd1)
- All traders were blind from 9 AM until fix at ~11:28 AM; twc_morning ran at 11:15 and found 0 candidates → force-scan at 11:28 placed 7 orders successfully

### Lesson
Kalshi appears to be migrating away from integer-cent fields to dollar-string fields across the entire API. Watch for the same pattern in any endpoint we haven't audited yet. Key untested: any market list field that uses prices (e.g. `last_price_dollars`, `previous_yes_ask_dollars` visible in raw response).

## 9 AM Settler Results (March 11 trades)
- 129 settled | 66W/63L | net **−$29.78** (paper)
- `twc_intraday`: 1W/5L (−$9.04) — DAL, NOLA, ATL, MIN, DC all high calls missed
- `twc_low_dip`: 6W/6L (+$4.60)
- `twc_morning`: 3W/3L (−$7.38)

## 7-day P&L (correct, as of morning March 12)
Best: `ensemble_twc` +$401 (62% win), `twc_low_dip` +$328 (43%), `twc_icon_adjusted` +$212 (69%)
Worst: `nws_forecast` −$607 (11% win), `twc_forecast` −$418, `nws_obs_confirmed` −$354 (25%)
`twc_morning`: +$40 (88% win) — only 25 settled, small sample but sharp
`cli_confirmed`: +$18 (100%) — only 4 trades, very small sample

## `_tier_to_tags` NameError — NOT systematic
- Only 2 isolated errors on March 11 afternoon (LAX + LAS observations)
- Module loads clean, `_tier_to_tags` is properly in namespace
- Cannot reproduce with simulated events
- Not a broken import; likely transient edge case in specific market state

## twc_morning_trader: Open Code Review Finding (HIGH, from report 2026-03-02)
- `fill_price = sim_price / 100.0` bug in `twc_icon_adjusted_trader.py` line ~115
- `simulate_fill()` already returns a fraction; dividing again makes fill prices 100× too small
- Corrupts P&L and edge calibration for `twc_icon_adjusted` model
- NOT YET FIXED as of this session

## twc_morning_trader: Stale "3 PM deadline" comments
- `DEADLINE_HOUR_ET = 22` (10 PM ET) but docstrings/print statements say "3 PM ET" in several places
- Cosmetic — behavior is correct, comments are wrong
- Low priority

## twc_morning_trader: `effective_max` cap — INTENTIONAL per Ian
- `effective_max = min(open_ask + 0.01, o["max_limit"])` limits max bid to 1¢ above opening ask
- Prevents chasing markets that moved up significantly from placement
- Ian confirmed this is intentional design

---

## nbm_forecast Model Audit (March 12, ~noon)

### Bugs fixed
1. `settle_trades()` had 3 bugs (manual settle path only — settle_all.py was fine):
   - Wrong arg count: `Trade.settle(id, outcome, pnl)` → `Trade.settle(id, cli_val, won, pnl)` (commit b19f02e)
   - Wrong CLI key: `cli.get("cli_high")` → `cli.get("high")` (load_cli_actuals returns "high"/"low")
   - `trade.entry_p` doesn't exist → `trade.fill_price or trade.market_price` (commit 86cb6d8)

### March 11 expired trades — unexplained
- All 30 nbm_forecast March 11 trades settled as `status='expired'` at 2026-03-11 17:13:22 UTC (9:13 AM ET)
- That's the settler daemon run time
- `Trade.expire()` has `WHERE status='pending_fill'` guard — shouldn't touch `assumed` trades
- `Trade.settle()` should only produce 'won'/'lost' — not 'expired'
- Could not identify the code path. If it recurs on March 12, investigate further.

### Performance: 10% win rate (n=165 settled trades)
- avg our_p=31%, avg mkt_p=14%, actual win rate=10%
- Market was RIGHT on YES/greater direction 92% of the time

### Root cause: sigma too small
- NBM XND-derived σ avg = 2.3°F
- Actual CLI forecast error std dev = 5.28°F
- Model is ~2.3x overconfident in its probability estimates
- Applied `SIGMA_INFLATION = 2.3` constant, inflates nbm_sd before computing truncnorm probabilities
- ⚠️ BUT: inflation is not straightforwardly helpful in all cases — see below

### ⚠️ Sigma inflation may not be the right fix
- For thresholds ABOVE the NBM prediction (e.g. Dallas: NBM=78, threshold=85): inflation INCREASES p (16.8% → 39.7%) — makes it WORSE vs market at 4%
- For thresholds AT the mean: p stays ~50% regardless of σ
- Inflation only helps when threshold is BELOW the mean AND model is over-pricing it
- The real problem: NBM point forecasts have high uncertainty that isn't captured by XND
- Need to revisit this — inflation as applied may cause more harm than good
- Was in middle of investigating when session compacted

### Next steps for nbm_forecast
- Revert or refine SIGMA_INFLATION — raw 2.3x may not be the right fix
- Consider whether the model has fundamental edge at all (market correct 92% on direction)
- May need to move to relative edge sizing — only trade when our_p >> mkt by large margin
- Or abandon nbm_forecast if no systematic edge exists vs the market

### Key stats by bucket
- our_p ~10%: 0W/11L (market correct — no trades here)
- our_p ~20%: 4W/66L (6% actual — market pricing ~13% was closer)
- our_p ~40%: 10W/21L (32% actual — closest to fair value at this bucket)
- All directions (YES/NO) showing ~10% win rate — no directional edge found


---

## twc_low_dip Trader Analysis (afternoon, March 12)

### Performance summary (26 settled trades, Mar 9-11)
- W=12 L=14 WR=46% net=+$332.57
- **$297 Philly 1¢ YES/less win** = 89% of total P&L; stripped: +$39 on 25 trades
- By category: no/between 67% WR +$55; no/greater 25% WR −$1; no/less 0% WR −$6; yes/greater 0% WR −$6; yes/less 33% WR +$291

### Fill filter analysis (excluding Philly outlier)
- Trades with fill ≤ 50¢: best $/trade at +$1.86 (43% WR)
- Trades with fill > 50¢: much worse (52-74¢ fills are bad payouts)
- **50¢ cap on entry is the right call** — confirmed by data

### Orders placed vs filled
- Total: 132 orders placed, 35 filled (27% fill rate), 72 expired, 25 pending
- Fill rates roughly uniform across confidence buckets (~23-35%)
- Avg fill price overall: 31¢ (reasonable)

### Key finding: 72 expired orders, ALL would have won ($216 missed profit)
- 0 would-have-lost — the TWC signal was right on 100% of expired orders
- Two distinct groups:
  1. **39 "market disagreed" orders (YES < 20%)**: Market priced NO at 80-99¢ (avg 93¢). Our limit 70-74¢. Unfillable without paying near-full market price. Market was confident YES would happen; TWC said otherwise and was right. Can't easily capture via limit orders.
  2. **21 "market agreed" orders (YES ≥ 50%, all March 11)**: NO was cheap at 26-37¢. Our limit was 63-74¢. Should have filled immediately. Likely cause: wide bid-ask spread (YES bid << YES ask), so computed NO ask = 100 - YES bid was just above our limit by 1-2¢.

### Recommended fix (pending Ian approval)
At order placement: check current NO ask. If NO ask ≤ 50¢, fill at market immediately (not resting limit). If NO ask > 50¢, place resting 50¢ limit. This:
- Captures the 21 "easy wins" that slipped through
- Keeps 50¢ cap for bad-payout protection
- Doesn't overpay for the 39 hard-fill situations

### Per-city over-ordering
- 4-5 correlated orders per city per day (all betting same temperature outcome)
- 75% expire unfilled — mostly harmless
- But on frontal days, multiple correlated orders CAN fill (Philly Mar11: 3 correlated fills)
- Recommended: cap at 2 orders per city per day (highest-Δ greater/less + highest-Δ between)
- Pending Ian decision

### fill_monitor status for twc_low_dip
- fill_monitor IS processing these (status=pending_fill → processes → expires correctly)
- Uses `trade.market_price` as NO limit in cents
- Checks: (1) NO ask from orderbook ≤ limit, (2) tape trades where no_price ≤ limit_cents
- The 21 March 11 non-fills: fill_monitor ran, but NO ask (= 100 - YES bid) was just above limit

