# Session Notes — 2026-03-22

## Resolution Rules Arb Scan (with Ian, late night Mar 21)

### What we did
- Ran a full scan of Kalshi markets for arb opportunities using the resolution-rules-arb skill
- Compared Kalshi vs Polymarket on the tech layoffs market
- Analyzed WTA Miami Open markets on both platforms

### Key findings

**Real arbs found but poor ROI:**
- KXIPOSTARLINK monotonicity: Oct26 bid 6¢ > Nov26 ask 4¢, but only 1 contract at that price. Not meaningful.
- KXGREENLAND subset violation: KXGREENLAND-29-27 (purchase from Denmark, no announcement trigger) ask 12¢ > KXGREENTERRITORY-29-27 (any acquisition incl. announcement trigger) ask 10¢. True arb +1¢/contract but terrible ROI over multi-year horizon.
- KXGREENLANDPRICE exhaustive bucket: sum of YES bids = 91¢ < $1 guaranteed → +9¢ but locked ~3 years.
- None cleared the 300% annualized bar.

**Kalshi vs Polymarket — Tech Layoffs (KXLAYOFFSYINFO-26):**
- Kalshi YES = 87.5¢, Polymarket "Up" = 75¢
- Both use FRED series JTU5100LDL (NSA, confirmed via Kalshi series endpoint settlement_sources)
- Near-arb: Buy Poly Up (75¢) + Buy Kalshi NO (12.5¢) = 87.5¢ total → +12.5¢ if both resolve same
- P(diverge) ~2-3% (revision timing, exact-tie at 447k). Strong EV but not guaranteed.
- Blocker: no funded Polymarket account.
- The correct arb structure: buy YES on cheap platform + buy NO on expensive platform < $1

**WTA Miami Open:**
- Kalshi only has real price discovery. Polymarket has untouched market maker quotes (~40%) for most players.
- Swiatek (#2 seed) eliminated R2 by Linette — explains her 1¢ Kalshi price.
- Pegula, Anisimova, Mboko all still in as of Mar 22 00:00 UTC (R3 starting).
- Sabalenka/Polymarket 32.5¢ ≈ Kalshi 33¢ — only consistent cross-platform price.
- Rybakina: Poly 12.5¢ vs Kalshi 18¢ — only meaningful cross-platform gap with real volume on both.
- No viable arb; Polymarket illiquid for most players.

### Lessons learned / mistakes made
- **API truncation wasn't truncation** — Kalshi `rules_primary` field doesn't include settlement source. That lives at `/series/{ticker}` under `settlement_sources`. Always fetch series endpoint for full resolution info.
- **Always check full orderbook depth**, not just top bid. Oct26 bid had 675 contracts at 3¢ but top bid was 1 contract at 6¢.
- **Cross-platform arb structure**: buy YES on cheap + buy NO on expensive < $1 = profit. Check both directions.
- **Near-arb is valid**: P(same resolution) ~96-98% with strong EV should be sized as high-confidence trade, not dismissed.
- **Product_metadata notices have expiry dates** — check the deadline before flagging as a current risk.
- **In-progress tournament markets**: check current standings before assuming underdog prices represent fair value. Many 1¢ players were already eliminated.

## WTA Miami Open — Polymarket Liquidity Provision Play

**Ian's insight (correct):** The ~40¢ Polymarket prices for most players are ASK-side (market maker). The BID side is near zero / nonexistent. This means you can post limit bids at 1-3¢ on players still alive with real win probability and be the best bid in an empty book.

**The play:**
- Post limit buys at 1-3¢ on active top-seeded players still in R3 (Anisimova, Mboko, Pegula, Muchova, etc.)
- Fair value for a top-10 seed still in is ~5-15%
- If anyone sells into your bid: huge edge (buy 3¢, fair value 10-15¢)
- If never filled: zero cost, zero risk

**Blockers:**
1. Polymarket requires Polygon wallet + USDC.e — not set up yet
2. Fill probability unknown — need actual CLOB orderbook depth (can pull via Polymarket CLOB API)
3. Tournament ends March 29 — window closing fast

**Takeaway:** Setting up Polymarket access is worth it for situations like this. Thin tournament markets with empty bid books are recurring opportunities.

## Skill / Config Changes Made

### resolution-rules-arb skill installed
- Location: `/data/workspace/skills/resolution-rules-arb/SKILL.md` + `references/patterns.md`
- Also updated the .skill zip at `/data/workspace/resolution-rules-arb.skill`
- Key additions to skill: series endpoint fetching, correct arb formula, near-arb framework, full orderbook depth instructions

### OpenClaw config updated
- Added `skills.load.extraDirs: ["/data/workspace/skills"]` to `/data/.openclaw/openclaw.json`
- Gateway HUP'd to reload config (new PID 2453177)
- This makes `/data/workspace/skills/` auto-discoverable — both `agent-browser` and `resolution-rules-arb` should now appear in `<available_skills>` on next session
- `/openclaw/skills/` = system bundled skills (auto, not ours, gets wiped on redeploy)
- `/data/workspace/skills/` = user skills (persistent in /data/, our stuff goes here)

---

## Sports Arb — match_team() Normalization Fix (Mar 22 morning session)

### Changes made (commits c6efe68, 5eff28b)
- Added `_ascii_fold()` helper: strips Unicode accents (NFD decompose → drop Mn), converts hyphens/dashes to spaces
- Applied in `match_team()` at three points:
  1. Strategy 1 (exact): fold both lookup and Vegas name before comparing
  2. Strategy 1b (new, alpha-only): strip ALL non-alpha chars, compare → catches "Al-Selwady" == "Alselwady"
  3. Strategy 2 (word-subset): fold Vegas name before splitting into vwords; `_norm_word` also calls `_ascii_fold` on each word
- The `_norm_word` double-fold is safe — `_ascii_fold` is idempotent
- Fold applied BEFORE `split()` so hyphens become spaces and create separate words correctly

### Verified working (all three known failure cases)
- `Montréal Canadiens` → matches `Montreal Canadiens` ✅ (strategy 1 after fold)
- `Abdul-Kareem Al-Selwady` → matches `Abdul Kareem Alselwady` ✅ (strategy 1b alpha-only)
- `CHA` bilateral: abb_map already correct (Charlotte Hornets); bilateral matching rejects Boston+Memphis pair ✅

### No regressions
- `NC State` → `NC State Wolfpack` (word-subset) ✅
- `Golden State` → `Golden State Warriors` ✅
- `Canadiens` → mascot match → `Montreal Canadiens` ✅
- `SJ Sharks` → mascot match → `San Jose Sharks` ✅
- `Boston Red Sox` → no match (not in games) ✅
- `Carolina` → correctly matched `North Carolina Tar Heels` (away side) ✅

### Testing lesson: don't print g["home_team"] as the matched result
- A test appeared to show "Carolina → Miami Hurricanes" but it was printing the HOME team of the matched game
- Correct display: `g[f"{side}_team"]` — the actual matched team was North Carolina Tar Heels (away)
- Always verify WHICH team was matched, not just which game

### TEAM_ALIASES approach rejected for CHA
- Added then removed alias `"CHA": "Charlotte Hornets"` — TEAM_ALIASES maps full team names, not abbreviations
- abb_map["nba"]["CHA"] was already "Charlotte Hornets" — no alias needed
- The original wrong match (Boston Celtics for CHA) was a bug in old single-team code James already fixed with bilateral matching

---

## cli_confirmed Trader — Diagnosis & Fixes (late night Mar 21 / early Mar 22)

### Root cause of week 11 decline
- SFO duplicate trades bug: dedup key used `(hour // 6) * 6`, so events at 05:58Z (key: `6h0`) and 06:01Z (key: `6h6`) both passed — same underlying reading, two fire events
- SFO March 18 spike anomaly: `six_hour_high` field in the :54 METAR reported 85°F vs running max of 81°F (+4°F). Anomalous METAR 1SNNN field value.
- **All cli_confirmed trades are paper** (`is_paper=1`). Live P&L exposure only -$18.62 across 15 trades.

### Fixes shipped (commits a74d315 → b1e1fe3)

**wethr_push_client.py:**
- six_hour_high now only fires when `six_high_f <= cur_high + 1` (was: always when `six_high_f > cur_high`)
- Spike logged (⚠️) and skipped when `six_high_f > cur_high + 1`
- Threshold: +1 allows normal ASOS disambiguation; SFO case was +4, well outside range
- **PENDING DISCUSSION**: Ian pushed back on the +1 constant — he wants us to look at the data first, determine the max normal disambiguation delta empirically, then set the threshold

**cli_push_trigger.py:**
- Per-city-per-day dedup added (any source — CLI, DSM, six_hour_high): city fires at most once per day
- `_update_city_state` now called when six_hour_high fires (previously it wasn't, so CLI events for same city the same day were not blocked)
- `confirmed_high is None` guard added to six_hour_high path (prevents TypeError)

### Key insight: six_hour_high mechanism
- new_high events fire throughout the day as temperatures rise (well before synoptic boundary)
- six_hour_high fires LATER at the 00Z/06Z/12Z/18Z boundary, providing an EXACT 0.1°C reading
- The six_hour_high's primary value is **disambiguation**: ASOS ambiguity means new_high might store the lower bound (e.g. 71°F for 22°C), and six_hour_high confirms it was actually 72°F
- Normal pattern: six_high_f = cur_high + 0 or +1 (disambiguation)
- SFO March 18 anomaly: +4°F → true spike in the METAR 1SNNN field

### Data findings (KNYC local-day comparison)
- March 19: max individual obs = 43°F, push_extremes = 44°F → six_hour_high provided +1°F
- March 20: max individual obs = 55.9°F, push_extremes = 57°F → +1°F
- March 21: max individual obs = 57°F, push_extremes = 59°F → +2°F
- KMDW: obs and push_extremes typically match within 1°F
- SFO March 18: six_hour_high was +4°F above push_extremes → clear anomaly

### Open question (Ian to weigh in)
- Normal disambiguation delta appears to be at most +2°F based on data
- SFO spike was +4°F
- Current code uses +1 threshold → Ian says that's not the right approach
- Waiting for Ian's call on the correct threshold or mechanism before final fix

---

## obs_peak_model_trader (v9) — Deep Diagnosis (Mar 22 ~00:00–09:30 UTC)

### What we established
- Trader IS running v9 (not v8b). The `peak_passed_v8b` tags are from before March 16. Header comment just wasn't updated.
- TWC_PEAK_MODEL evaluator also runs inside the same trader as a separate signal.

### Root cause 1 — Dedup failure (351 rejected trades)
- `Trade.exists_for_model` only checks `status IN ('pending','pending_fill')`
- FOK orders that get rejected → `status='rejected'` → not blocked → same market retried every 5-min push event
- Worst ticker: `KXHIGHTSFO-26MAR18-T79` appeared 63 times
- The rejections themselves cost no money (FOK = clean reject, no exposure)
- **Real harm**: wasted API calls + DB clutter + misleading stats

### Root cause 2 — Time gate not applied to push events
- The scan has `if not (10.0 <= local_h <= 21.5): continue` — push events don't
- `on_peak_push_event` calls `_evaluate_city` directly with zero time filtering
- Wethr push events at 8am trigger v9 before the daily peak has formed
- Gate trader doesn't have this problem — its triple-condition gate (30+ min since obs peak, ≥1°C decline, 60+ min past OM peak) naturally prevents early firing

### Root cause 3 — `record_rejected` hardcodes our_p=0.0
- `Trade.record_rejected` stores `our_probability=0.0` with comment "# not in TradeRequest" — it IS in TradeRequest
- Cosmetic bug: actual trading computation was correct, only DB record is misleading
- All 351 rejected records show `our_p=0.00`

### Root cause 4 — Model applied to wrong situation (`less/YES` with high p2plus)
- **The actual cause of losses**: the model is being applied to `less/YES` markets where p2plus is still high (0.50–0.93)
- When p2plus > 0.20: model is saying "lots of upside likely" but we compute p0=5-43% and trade anyway if `p0 - market_price ≥ MIN_EDGE`
- **All 4 wins**: p2plus ≤ 0.12 (model genuinely confident peak is in)
- **Almost all losses**: p2plus 0.28–0.93 (model uncertain, still trading)
- Fix: gate `less/YES` on `p2plus < 0.20` before computing our_p → don't trade when model is saying upside is likely

### Training data — clarifications
- Training uses hourly sampling (`for hour_t in range(24)`) NOT push events — no sampling bias
- class 0 rate at h=8 is 12.84% (overall). Spring: 14.1%. Winter: 18.8%. Not dramatically different.
- The model IS correctly calibrated for local_hour. The p2plus signal is the discriminator.

### The correct model interpretation for `less/YES`
- When p2plus is low (< 0.20): model confident the high is in → good signal → trade
- When p2plus is high: model is uncertain/wrong scenario — the question "will high stay below cap?" can't be answered by bucket-level class 0/1 probabilities alone when there's still 80%+ chance of further upside

### Wins vs losses pattern
| Outcome | mp | p0 | p2plus | Pattern |
|---|---|---|---|---|
| All 4 wins | 0.34–0.87 | 0.79–0.90 | 0.05–0.12 | Model + market agree, low p2plus |
| Almost all losses | 0.01–0.37 | 0.03–0.43 | 0.28–0.93 | High p2plus, low market price |
| Houston outlier loss | 0.69 | 0.79 | 0.11 | p2plus ok but market agreed and still lost — normal variance |

### Fixes needed (not yet implemented)
1. **Dedup fix**: extend `Trade.exists_for_model` to also check `status='rejected'`
2. **Time gate**: add ≥10am local gate to `on_peak_push_event`
3. **p2plus gate**: add `if p2plus > 0.20: skip` for `less/YES` trades in `_evaluate_peak_v9`
4. **`record_rejected` bug**: store actual `our_probability` and `edge` from req (cosmetic but useful for debugging)

### Ian's points
- FOK rejections are fine (no money lost) — the dedup bug just creates clutter, not losses
- IOC/partial fills rejected as alternative — risk of getting stuck in a bad position if market moves against you
- Model should beat retail traders — it can, but only when applied correctly (low p2plus, confirmed peak in)
- Contract cap / IOC rejected — correct answer is fix the dedup so we stop retrying

---

## obs_peak_model_trader — Settlement Bug (Mar 22 afternoon)

### What Ian found
- Ian asked about trade ID 8661: KXHIGHCHI-26MAR21-T63, dashboard showed "Chicago High 64°F or above" WON +$377.19
- Actual Kalshi market: `strike_type=less`, `cap_strike=63` — "Will Chicago high be <63°F?"
- Kalshi result: `no` (Chicago hit 77°F on March 21). The YES bet should have LOST.

### Root cause chain
1. `obs_peak_model_trader.py`'s `TradeRequest` call omits `strike_type`, `floor_strike`, `cap_strike`
2. `open_filled()` in `db.py` falls back to `_parse_ticker_meta(ticker)` — which always returns `strike_type="greater"` for T-prefix tickers (T63, T76, etc.) because the ticker is ambiguous
3. `settle_all.py` settles before Kalshi finalizes → uses CLI fallback → `_resolve_yes_won()` reads DB value (`strike_type=greater`, `floor_strike=63`)
4. `cli_actual=77 > 63` → YES wins → phantom WIN

### Audit results
- **11 phantom wins**: Kalshi says result=no (YES lost) but settled as WON
- **4 missed wins**: Kalshi says result=yes (YES won) but settled as LOST
- **True P&L**: -$499.20 (stored was +$643.03)
- Pattern: only affects `less` markets (all recent T-prefix trades from obs_peak_model)
- `between` markets (B-prefix) were settled correctly throughout

### Fix applied (commit pending)
- Added `strike_type=strike_type, floor_strike=floor_strike, cap_strike=cap_strike` to the `TradeRequest` in `obs_peak_model_trader.py`
- Local variables already existed — just weren't being passed
- Syntax checked OK

### DB correction pending (awaiting Ian approval)
- 11 phantom wins to void and correct: IDs 6236, 8308, 8317, 8347, 8661, 8701, 8786, 8788, 8812 (plus 8703, 8984 as missed wins)
- Need approval before touching settled records

### Broader implication
- Model is actually significantly losing (-$499 real). The obs_peak_model v9 is NOT working yet.
- See earlier session notes for root cause analysis (p2plus gate needed, dedup fix needed, time gate needed)

### Bad trades from March 21 (the big "wins" that are actually losses)
All these are `less` markets with cli_actual > cap_strike — should have lost, settled as won:
8308 BOS T50 stored +$23.82 → actual -$8.37
8316 ATL T80 stored +$97.32 → actual -$3.01 (wasn't in phantom win list, but same pattern — need to re-check)
8317 MIA T75 stored +$208.89 → actual -$2.11
8347 DAL T90 stored +$62.95 → actual -$6.90
8356 SFO T66 stored +$55.02 → actual -$2.25 (not in list above, check)
8661 CHI T63 stored +$377.19 → actual -$3.81
8701 SATX T90 stored +$85.26 → actual -$1.74
8786 AUS T91 stored +$60.12 → actual -$6.68
8788 OKC T93 stored +$56.49 → actual -$5.18
8812 PHX T103 stored +$38.98 → actual -$1.96

---

## P_TABLE Calibration Analysis (Mar 22 afternoon)

### What we did
- Ran fill-price vs actual win rate analysis on 105 settled twc_morning trades
- Key finding: 65-74¢ fill bucket = 47% WR (P_TABLE said 90%) — massive adverse selection
- 75-84¢ fill bucket = 93% WR (P_TABLE said 92.5%) — genuine edge, market wrong

### Calibration table (3,282 training rows, instant from existing cache)
Built using generate_training_rows() from build_forecast_model.py — NO new API calls needed.

Key findings:
- no/between 65-70¢: 74.4% WR vs 67.5% market → +6.9% edge ✅ (N=125)
- no/between 70-75¢: 80.0% WR vs 72.5% market → +7.5% edge ✅ (N=245)
- no/between 75-80¢: 80.4% WR, +2.9% edge (N=260)
- no/greater 65-75¢: 50-64% WR vs 67-72% market → NEGATIVE edge ❌ (N=8-11)
- no/greater 75-80¢: 88.9% WR, +11.4% edge (N=18, small)
- At 90¢+: market well-calibrated, near-zero edge across all types

### The bad mistake
Ian asked for calibration table → I spawned James to fetch 60k Kalshi markets → burned 20 req/s rate limit → disrupted live traders → all data was already in existing training rows.
- Rule written to LEARNINGS.md Part 1: check existing data first, always
- MEMORY.md duplicate removed (belongs in LEARNINGS only)

### Proposed new P_TABLE approach (in progress)
Replace delta-based P_TABLE with market-price-conditioned calibration table:
- Entry condition: `calibrated_P(type, market_price_bucket) - market_price > MIN_SPREAD`
- Delta disappears as input (market price already encodes it)
- Ian's questions pending:
  1. Keep delta as hard floor filter, or drop entirely?
  2. Small-N buckets (N<20): fall back to adjacent, require min N=30, or "don't trade"?

### Status
- Calibration analysis complete, table computed
- Adjusted P_TABLE build pending Ian's answers to 2 questions

---

## cli_confirmed — Final Spike Filter + Dedup Cleanup (Mar 22 00:00–09:30 UTC)

### Six_hour_high spike filter — final design
- Previous code: `six_high_f > cur_high` → write & dispatch. Wrong — fires on spike.
- Revised: `six_high_f == cur_high` → fire. `six_high_f > cur_high` → log ⚠️ spike + skip.
- Also log if `six_high_f < cur_high` (stale — skip silently).
- **Threshold**: strict equality. Data shows new_high events always fire before six_hour_high at synoptic boundary (new_high carries exact NWS-reported °F, not °C-converted). Zero +1 disambiguation cases seen in production.
- **KSFO added to `SIX_HOUR_BLOCKED_STATIONS`**: SFO completely disabled for six_hour_high. Only CLI/DSM can trigger SFO trades. Easier to extend if other anomalous stations emerge.

### Stale guard added to ALL sources in cli_push_trigger
- Previously only six_hour_high had a stale guard (confirmed_high < obs_running_max → skip)
- Added same guard to CLI and DSM paths for consistency

### DB cleanup (commit 250137f)
- **12 SFO March 18 spike trades deleted** (paper, based on bad 85°F data)
- **94 paper duplicate trades deleted** (same ticker traded 2–25x from repeated city fires)
- **28 live BOS March 1 records left intact** — real Kalshi fills, accurate history of pre-fix behavior
- **27 BOS duplicates later deleted** (were live but all paper except 7 filled BOS which are real history)

### BOS March 1 P&L correction (commit 71c67e6)
- 27 live BOS duplicate records deleted (7 were filled at -$3 each = -$21 real money lost before fix)
- `_repair_portfolios` called to recompute — balance: $1006.65 → $1027.65, realized_pnl: $156.65 → $177.65
- Formula: `balance = starting_balance + realized_pnl - pending_exposure` (never just starting + pnl)
- Called `_repair_portfolios` rather than manual UPDATE — correct approach, always use this

### Portfolio P&L — before vs after cleanup
- Before ALL cleanup: realized_pnl ≈ -$24 (negative — SFO spike trades had net -$181 P&L)
- After removing SFO spike + BOS dups: realized_pnl = +$177.65
- As of Mar 22 16:45: realized_pnl = +$192.63, 40W/2L on 42 settled trades

### Two losses explained
- Both March 14, NYC (old code, old notes format)
- Confirmed=50°F from preliminary CLI, but final CLI came in at 51°F (day not done when gate fired)
- Inherent strategy risk: preliminary confirmation can be below final if temps still rising
- Not a code bug — gate timing risk

---

## cli_confirmed — Metadata Fix (commit fd4fea2, Mar 22 ~16:45 UTC)

### What was wrong
- `our_probability` = 0.0 on all trades (TradeRequest defaults, never set by place_paper_order)
- `edge` = -market_price (meaningless, just the default)
- `metadata` = {} on every trade (never populated)
- `notes` field had correct info as human-readable string but not machine-queryable

### Fix
- `our_probability` = 1.0 (confirmed trades — we know the peak is set)
- `edge` = 1.0 - entry_price
- `metadata` now stores: `{source, confirmed_high, valid_hour, om_peak_h, peak_local_h, gate_lead_min, gate_pre_confirmed}`
- Future SQL: `WHERE json_extract(metadata, '$.source') = 'six_hour_high'`, group by source, correlate gate_lead_min with win rate

### Trader logic verified correct
- Reviewed last 12 settled trades: all wins check out mechanically
- OKC conf=96°F → B95.5 YES ✓, B97.5 NO ✓; PHX conf=105°F → B105.5 YES ✓, B107.5 NO ✓; etc.
- Two losses (March 14 NYC) were preliminary-vs-final CLI discrepancy, not logic bug

---

## P_TABLE Calibration — Regression Fixed-Point Analysis (Mar 22 ~17:00–17:30 UTC)

### What we tried
Attempted Option B: fit logistic regression on all training rows → solve P* = regression(delta, P*−MIN_EDGE, type) numerically via brentq.

### First attempt failure
- Included yes/between rows (delta=0, WR=33%) in training → `is_no` coefficient learned wrong sign → all NO cells snapped to 0.090 ("never trade"), all YES cells to 0.999. Garbage.

### Second attempt (correct: exclude yes/between)
- Training set: 2772 rows (excl yes/between — we never trade them, they distort model)
- Log loss: 0.3392 vs baseline 0.3855 — good fit
- Coefficients: delta×noon (+0.9942) dominant, noon (+0.5760), is_no (-0.1241), delta×no (-0.1838)

### Fixed-point result
- **46/64 cells → "never-trade" (P*≈0.09)** — ALL NO cells
- **14/64 cells → clean P*** — ALL YES cells (yes/greater and yes/less)
- This is NOT a model bug. The "never-trade" result for NO is CORRECT given MIN_EDGE=0.08.

### Key insight: NO trades have thin edge
Direct edge scan result:
- NO/between/below delta=1: max edge = **-1.1%** (actually negative!)
- NO/between/below delta=2: max edge = **+2.2%** at noon=0.76
- NO/between/below delta=3: max edge = **+4.4%** at noon=0.76
- NO/between/below delta=4: max edge = **+6.1%** at noon=0.77
- NO/between/below delta=5: max edge = **+7.4%** at noon=0.77

None clear MIN_EDGE=0.08 (8%) except barely at delta=5.

**YES/less trades**: max edge = **24–30%** consistently across all deltas, peaking at noon=0.51–0.54. Huge genuine alpha.

### Why this confirms our trading results
- NO trades in practice: 68% WR, -$100 net — market is well-calibrated for NO, thin edge
- YES trades in practice: 100% WR on legitimate trades, +$126 net — market systematically underprices YES
- Our edge comes from YES trades, not NO trades. P_TABLE has been overstating NO edge.

### Practical implication
- To find NO fixed-points, would need MIN_EDGE ≈ 0.03–0.04 (3-4%)
- YES fixed-points exist cleanly: yes/less and yes/greater converge at P*=0.94–0.99

### Status
- Analysis complete. No implementation yet. Awaiting Ian decision on what to do with NO trades.

---

## Session continued — 2026-03-22 afternoon (UTC)

### 429 Rate Limit Investigation

Root cause of twc_pace 429s: `twc_portfolio_trader._build_candidates()` was calling `get_best_ask()` → `fetch_orderbook()` (GET /markets/{ticker}/orderbook) for EVERY candidate market × 2 (yes + no) EVERY 10-second loop. This was N×2 Kalshi API calls per 10s = potentially 6+/sec constant background load.

When twc_pace fired its 20 sequential market fetches on top of that, the combined rate tipped over.

**Ian separately caused 429s on his end via a large historical data pull — that was the immediate trigger for the first run's failures.**

### Fixes applied

1. **btc15m_paper_trader**: added window gate — only hits Kalshi API during first 95s of each 15-min window (windows open at :00/:15/:30/:45). Reduces calls by ~89%. (commit f8704a9)

2. **twc_portfolio_trader cached ask bug**: I incorrectly "fixed" the 429s by replacing `get_best_ask()` with `m.get("yes_ask")` from the 3-hour market cache. Ian caught this immediately — market prices change in seconds, 3h stale ask = wrong edge = wrong trades. **Reverted immediately.** (commit e4126e2)

3. **LEARNINGS.md rule added**: "Never cache yes_ask / no_ask — market prices change in seconds. Always call get_best_ask() / fetch_orderbook() at moment of trade decision." (commit 80c9cd7)

4. **"Never Make Uncached API Calls Inside Hot Loops" rule rewritten**: Ian pointed out this rule was wrong/too broad. Real concerns are (a) calls/sec vs rate limit and (b) data freshness. Rewrote to: "Know your call rate before shipping any API loop — calculate calls/min, cache data that doesn't change within the relevant window, never cache prices." (commit aad5ffa)

### twc_portfolio 429 still open

The underlying issue — `get_best_ask()` called N×2 per 10s loop — is not yet fixed. Correct fix is either:
- Short-TTL orderbook cache (30s) in `get_orderbook()` so repeated calls within a scan pass collapse into one
- OR: shorten `MARKET_REFRESH_SECS` from 3h to ~2min so `yes_ask_dollars`/`no_ask_dollars` from the bulk `/markets` response are fresh enough to use (eliminating per-ticker orderbook calls entirely)

Ian asked what exactly it's fetching (GET /markets/{ticker}/orderbook = full depth). Pointed out that `/markets` list response already includes `yes_ask_dollars`/`no_ask_dollars` at top of book. Discussion ongoing.

### Key rule reinforced
**Never cache yes_ask / no_ask and use the cached value for a trading decision.** Now written in LEARNINGS.md. This was not written down before — that's why I forgot it and made the bad fix.

### Commits today (afternoon)
- f8704a9: fix 429s — btc15m gate + twc_pace sleep + (bad) portfolio cache fix
- e4126e2: revert portfolio cache change (stale prices)
- 80c9cd7: add rule — never cache yes_ask/no_ask
- aad5ffa: rewrite hot-loop API rule to be accurate

### Open items as of 18:00 UTC
1. twc_portfolio 429 fix — proper approach TBD (short-TTL cache OR faster market refresh)
2. Orchestrator 6 PM top-up — remove or keep? Ian never answered
3. Morning healthcheck cron job timed out (600s) — needs investigation
4. forecasts.jsonl + wethr_push.jsonl rotation policy — never decided
5. Mac node crash investigation (twc_bucket_low, Mar 18) — blocked, Mac not connected

---

## cli_confirmed — 06Z Midnight Boundary + DSM for_date Bugs (Mar 22 ~17:00–18:48 UTC)

### Bug 1: 06Z midnight boundary (cli_push_trigger.py)

**Root cause:** `_utc_to_city_valid_hour` applied `local_h % 24` normalization BEFORE the midnight check. At 05:54Z, Denver `local_h_raw = -1.1` → after `% 24` = `22.9` → `window_start = 16.9 > 0` → both checks defeated. CT/MT/PT cities fired using yesterday's afternoon highs.

**Affect:** 21 bad paper trades at 05:53–05:59 UTC March 22 (IDs 9080–9100, Denver, Phoenix, Seattle, Minneapolis, San Antonio). All deleted, portfolio repaired (balance unchanged — all pending, zero P&L impact).

**Fix (commit 245dc53):** Check `local_h_raw < 0` BEFORE any `% 24`. If negative → fires before city's LST midnight → yesterday's data → return None.

**Correct window table (all LST offsets, year-round):**

| Window | Fires UTC | ET (−5) | CT (−6) | MT (−7) | PT (−8) |
|---|---|---|---|---|---|
| 00Z | 23:54Z | ✅ 18:54 | ✅ 17:54 | ✅ 16:54 | ✅ 15:54 |
| **06Z** | **05:54Z** | **❌ window spans midnight** | **❌ fires before midnight** | **❌ fires before midnight** | **❌ fires before midnight** |
| 12Z | 11:54Z | ✅ 06:54 | ❌ window spans midnight | ❌ window spans midnight | ❌ window spans midnight |
| 18Z | 17:54Z | ✅ 12:54 | ✅ 11:54 | ✅ 10:54 | ✅ 09:54 |

**06Z is rejected for ALL US cities** — ET because the window spans local midnight, CT/MT/PT because the event fires before local midnight.

**LST vs DST:** All offsets are standard time (LST), never DST-adjusted. During DST season, the "today" window starts at 1am actual clock time (e.g., 1am EDT). Events from midnight-to-1am actual time have `local_h_raw < 0` → treated as yesterday. This is intentional and matches NWS/Kalshi settlement. Arizona (MST = UTC-7, no DST) uses -7 year-round, no special handling.

**Previous LEARNINGS entry was backwards** — said "ET cities only → reject the 00Z-05:51Z window." Now corrected to proper table in LEARNINGS.md.

---

### Bug 2: DSM/CLI for_date not checked before dispatch (wethr_push_client.py)

**Root cause:** DSM and CLI reports are sometimes published after UTC midnight. Their `for_date` field carries yesterday's date (e.g., `2026-03-21`), but `_write_trigger()` and `_dispatch()` were called unconditionally. `cli_push_trigger.py` reads from `push_trigger.json` (which doesn't include `for_date`) and uses `_today_et()` = March 22 → trades March 22 markets with yesterday's confirmed highs.

**Affected trades:**
- **9431**: Miami NO High 79-80°F — DSM for 2026-03-21 (high=81°F) fired at 15:20 UTC → traded March 22 market
- **9438**: Atlanta NO High 82-83°F — DSM for 2026-03-21 (high=84°F) fired at 15:28 UTC, wrote 84 into ATL push_extremes for March 22 → six_hour_high at 15:27 UTC saw `conf=84 == cur_high=84` → fired

**Symptom:** CLI handler already had this check (`nws_obs_trader.on_push_event` skips with "for_date=2026-03-21 != today=2026-03-22"). But `cli_push_trigger.py` reads from JSON file — no access to `for_date`.

**Fix (commit 747e521):** In `wethr_push_client.py`, for both CLI and DSM event handlers, add `today_lst = _lst_date(station)` check before calling `_write_trigger` and `_dispatch`. If `for_date != today_lst`: still write to push_extremes (correct historical record) but skip the trading dispatch.

**DB cleanup:** Deleted trades 9431 and 9438 (both paper, pending). Cleared `push_extremes` for KATL 2026-03-22 (was poisoned to 84°F from yesterday's DSM — real March 22 ATL high rebuilds from live new_high events).

**Both daemons restarted** with new code:
- `cli_push_trigger.py` (PID 1771566) — new log at `forecast_logs/cli_push_trigger-2026-03-22.log`
- `wethr_push_client.py` (PID 1821369) — running with DSM for_date fix

### Final portfolio state as of 18:48 UTC March 22
- balance = $2192.63, realized_pnl = $192.63, total_trades = 42 settled

### Key lessons
- `_write_push_extreme(station, for_date, ...)` uses the DSM's own date — correct
- `_write_trigger(station, ...)` + `_dispatch(...)` have NO date context — they fire for "today"
- Guard must live at wethr_push_client dispatch layer, NOT in downstream handlers
- The spike filter (two checks: `local_h_raw < 0` AND `window_start < 0`) requires BOTH because they catch different failure modes
- **Never restart both wethr_push_client + cli_push_trigger simultaneously** without checking if either was already running the fixed code — could orphan an open SSE connection slot for 20 min


---

## btc15m_momentum Paper Trade Review (Mar 22 ~17:30–20:34 UTC)

### Stats after 20 settled trades (as of ~18:00 UTC)
- 10W/10L, net P&L = -$81.62, stored balance = $418.38
- Expected WR from backtest: 67-78% — not enough trades to conclude edge is gone (noise)
- Two trades had anomalously small dollar_size ($21.12 and $7.84) — explained below

### Fill mechanics (fully understood now)
- Paper GTD trades use `Trade.open_pending` → **NO portfolio deduction at entry**
- fill_monitor detects fill via two modes:
  - **TAPE mode**: if a recent tape trade at ≤ our limit exists, uses that volume as fill quantity
  - **ORDERBOOK mode**: checks live orderbook for contracts available at ≤ our limit
- All fills correctly marked `fill_status='deferred'` after fill_monitor confirms
- settle_all skips `limit_pending` (unfilled). Settle_all uses Kalshi API finalized result for BTC markets (no CLI fallback) — works correctly

### The partial fill issue
- Trade 9068 ($21.12): TAPE fill @ 48¢, 44/104 contracts — tape trade at that moment was only 44 contracts
- Trade 9204 ($7.84): TAPE fill @ 49¢, 16/102 contracts — tape trade was only 16 contracts
- Most other fills: ORDERBOOK fills with 937–34,422 contracts available → full fill
- This is a paper simulation artifact. In live trading with a GTC posted at the ask, the full book depth would be hit (liquid market with 22k+ contracts available)
- Dollar_size is scaled DOWN to match actual fill: `contracts_filled × price_cents/100`

### Settlement timing
- After every-5-min settle_all was deployed: trades settle ~15-20 min after entry (15 min window + up to 5 min settle_all lag)
- Before (hourly): first trade settled 60+ min after window close (old behavior, now fixed)
- Balance accounting is correct: `balance = starting_balance + realized_pnl - pending_exposure` ✅

### Bid above ask discussion
Ian asked: should we bid higher to get better fills?
- Secondary band (ask ~0.48, WR ~50%): ROI at ask = +4%, at ask+2¢ = 0%. No room. Stay at ask.
- Primary band (ask ~0.63, WR ~78.6%): ROI at ask = +25%, at ask+2¢ = +21%. Can absorb it, but barely an issue (only 2/20 were partial fills).
- Better fix: the paper fill_monitor's TAPE mode is conservative. Live trading with GTC would hit full book depth anyway.
- Ian's counter: keeping $50/trade size is useful to stress-test fill realism. Agreed — don't drop to $25.
- Resolution: no change to pricing or sizing for now. Continue monitoring.

### Entry interval split (20 trades)
- t15: 1W/0L
- t30: 3W/1L (75%)
- t45: 4W/4L (50%)  
- t60: 0W/3L ← concerning, 0% WR at 45–60s mark
- t90: 2W/2L (50%)
Worth watching. Backtest edge was first-candle (window open). If t60 stays weak at 50+ trades, consider tightening window to 45s.

### Primary band (afternoon, 0.60-0.65) only fired twice: 0W/2L — too few to conclude anything
### Secondary band dominating sample — most triggers are atm_prior_no_* conditions

---

## P_TABLE Calibration — Full Dataset Backfill (Mar 22 ~18:43–20:38 UTC)

### Noon price cache backfill
- Missing dates: Feb 24–26, Mar 21 (had TWC forecasts + CLI actuals but no noon prices)
- Root cause: `events_fetched` flag prevented retry even when specific tickers returned empty
- Fixed by clearing events from `events_fetched` and re-fetching
- Result: 26 dates now cached (Feb 24–Mar 21), 3,855 training rows (up from 3,282/22 dates)
- "No noon price" skips: 558 → 194 remaining

### Remaining 194 skips — root cause
- 149 tickers: event WAS fetched but specific market had no candlestick data at 16:00 UTC window (illiquid at that specific time — no bids/asks)
- ~41,973 "never attempted": these are markets in the `markets` cache for dates outside our forecast range (pre-Feb 24) — not actually needed for training
- The 149 illiquid tickers are genuinely unfillable — market had zero order book activity at noon on trading day
- Ian asked "why are there any skips left?" — answer: inherently missing (no price discovery at that window)

### Raw calibration results (3,855 rows, 26 dates)
Run but not yet summarized post-backfill. Key pattern from 22-date run still holds:
- HIGH markets: mean gap = −0.140 (P_TABLE overstates WR by 14pp avg)
- LOW markets: mean gap = +0.018 (essentially correct)
- Worst cells: high/yes/less/1 (WR=12.5% vs P_TABLE=0.535), high/no/between/above/1 (WR=42% vs P_TABLE=0.745)
- Best cells: low/no/between/above/2 (WR=100% vs P_TABLE=0.874), low/yes/greater/1 (WR=75% vs P_TABLE=0.635)
- Notes writeup saved to: `/data/workspace/notes/p_table_selection_bias_calibration.md`

### Rate limit clarification
- Kalshi GET calls do NOT need Ian's go-ahead
- Self-limit background tasks to ≤3 req/s (`time.sleep(0.33)`)
- Updated MEMORY.md "Ask Before Acting" section + LEARNINGS.md to reflect this

### Pending: re-run calibration analysis with full 3,855-row dataset and update notes
