# Session Notes — 2026-03-24

## Sports Arb Trader — Final Fixes (from Mar 22 session, continued)

Post-compaction work from previous session carried forward. All changes committed and pushed to main.

Key commits:
- fetch_vegas_odds rewritten: no date filter, single call per sport
- find_gap_trades: takes flat list instead of dict
- abb_map built fresh each run (never loaded from stale disk file)
- Dead code removed (EPL TIE abb_map write block)
- Title guard threshold: len>3 → len>=3 (fixes USC/TCU false rejections)
- Docstring updated: 3% → 5%

All clean. Confirmed by reviewing live run logs.

## HondaCivic / Polymarket Research

Analyzed @HondaCivic on Polymarket. Key findings documented in STRATEGY_IDEAS.md:
- Primary strategy: obs-confirmed certainty buys (100¢) + early forecast entries on specific buckets
- Strategy 22: Wunderground data error exploit — see STRATEGY_IDEAS.md for full details and trade link

## Lessons (also in LEARNINGS.md Part 1)

Got called out twice for restating Ian's words as analysis without doing actual research. 
Fixed: second time actually pulled IEM ASOS data, found 23°C vs ≤15°C discrepancy, which was the real story.

The pattern to avoid: elaborating on user-provided facts with extra framing and presenting it as research.

---

## fill_monitor incremental fill fix (commit f3bbf8d)

Fixed fill_monitor to accumulate partial fills across sweeps instead of marking done after first partial.

- Added 3 new DB cols: `contracts_filled`, `contracts_needed`, `dollars_filled`
- Added `Trade.partial_fill()` — accumulates, keeps status='pending_fill'
- Updated `Trade.expire()` — if contracts_filled > 0, finalizes as real position
- Updated `_record_fill()` — accumulates across sweeps, only calls Trade.fill() when fully filled
- **Confirmed working**: PHX B96.5 accumulated 6 contracts over 5 sweeps, correctly expired at GTD

## between_bucket re-run (March 23 → 24 markets)

- Original 15:00 UTC run hit TWC API outage → 0 orders
- Manually re-ran at 20:43 UTC → 67 orders for March 24 (IDs 10573–10638+)
- Result: 7 filled, 60 expired (correct)

## twc_morning_nws_gate bankroll bug — fixed (commits ae3c163, 716f9df)

Two locations where live Kalshi balance ($1.13) was used instead of paper portfolio ($500):
1. scan_and_build_candidates() — added bankroll_override=bankroll if PAPER else None
2. place_initial_orders() — added available_override=bankroll if PAPER else None
Trader had never placed a trade since deployment. Now working.

## Evening cron updated

Added between_bucket and twc_morning_nws_gate to expected-traders list (job ID a82a1ea0).

## bootstrap.sh qmd fix (commit 7ecab66 / pushed 6cb9dea)

Old bootstrap checked for wrong collection name ("workspace") at wrong XDG path.
Fix: run qmd update+embed with correct OpenClaw paths on every startup.

## memory_search — partially broken

FTS (BM25 keyword search) only covers session transcripts (sessions-main collection).
Memory files (MEMORY.md, memory/*.md) are in vector index but NOT in FTS.
So memory_search returns empty on keyword queries even though files are indexed.
Vector search works but requires 640MB reranker model download — too slow.
Workaround: use `qmd search` via exec with --collection flag, or read files directly.
