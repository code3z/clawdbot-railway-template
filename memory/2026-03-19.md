# Memory — 2026-03-19

## Key Paths (confirmed on Railway)

- OpenClaw workspace: `/data/.openclaw/workspace/`
- Trading repo: `/data/workspace/trading/` ← correct (not `polymarket/` as noted Mar 17)
- Memory files: `/data/.openclaw/workspace/memory/`
- RULES.md, LEARNINGS.md, README.md, SKILLS.md: all in `/data/workspace/trading/`

## AGENTS.md Path Fix

All 8 startup files in AGENTS.md now use full absolute paths. Previous entries used
relative `polymarket/` paths (from macOS), which resolved to nothing on Railway — root cause
of 5 of 8 sessions today starting with no context.

Startup error rule added: if any file fails to load, output `[STARTUP ERROR: <list>]` and stop.
No-truncation rule reinforced for all startup files, not just LEARNINGS.md.

## Session Audit (Mar 19)

Reviewed all today's sessions for rule violations. Main findings:
- 5/8 interactive sessions started blank (no startup files read) due to wrong AGENTS.md paths
- `6b86a734` (16:37): only session that completed startup correctly (`[STARTUP COMPLETE: ...]`)
- `88f87cbc` (18:39): read startup files (8 blank tool turns before greeting) but skipped `[STARTUP COMPLETE]`
- No code violations found in trading logic itself beyond the paper/live issue below

## Critical Bug Fixed: twc_morning_nws_gate_trader.py

Built today by James (session `7f327eaa`). Spec said paper-only. Code cloned
`twc_morning_trader.py` including `place_limit_order()` call — would have placed live
Kalshi orders at 11:15 ET tomorrow. Reviewing session saw `"Mode: PAPER 📄"` label and
missed the live code path underneath.

**Fix applied:** Added `PAPER = True` flag; `if PAPER: continue` gates out live block in
`place_initial_orders()`; mode label is now dynamic `'PAPER 📄' if PAPER else 'LIVE 🔴'`.
Pattern matches `twc_portfolio_trader.py`. Flip `PAPER = False` to go live.

LEARNINGS.md updated with this pattern and what to check in future paper trader reviews.

## RULES.md

- Rule 2 (`Max $3/trade, max $6/market/day`) removed — outdated, doesn't match live trader sizing
- Rules renumbered 1–29

## Trading System Status

- `twc_morning_trader`: live, running daily at 11:15 ET ✓
- `twc_morning_nws_gate_trader`: paper (sim strategies only), running at 11:15 ET ✓
- `twc_portfolio_trader`: paper, running at 11:00 ET ✓
- `between_bucket_trader`: paper, running at noon LST ✓
- All traders on Railway, orchestrator managing scheduling via APScheduler
